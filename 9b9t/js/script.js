Ray095
ray095
В сети

FADE365 — 21.02.2025 23:05
Я то бота для дельта маркета за неделю написал
А это ведь просто бот + бд
Без реализации графики
Ray095 — 21.02.2025 23:06
Сделай мне что бы загрузка данных была в admin.html то что пользователь укажит
И что бы ты видел и я
FADE365 — 21.02.2025 23:06
Ок
Ray095 — 21.02.2025 23:06
А то там локально стоит
Ray095 — 21.02.2025 23:06
Завтра хочу
Все, я улетел на боковую
FADE365 — 21.02.2025 23:07
Давай
Спокойной
FADE365 — Вчера, в 01:02
Короче я настроил spring на работу с HTTPS
и вырезал нахрен H2 бд так как буду делать усё на json
короче просто реализовал request API для бд
FADE365 — Вчера, в 04:53
бд ворк
Изображение
Изображение
FADE365 — Вчера, в 05:57
Изображение
эээээм
https://9b9t.shop:8443/api/products
Ray095 — Вчера, в 08:42
,?
Ray095 — Вчера, в 10:48
Это я для себя
(убрать способ доставки по координатам ) что бы не забыть 
Изображение
Ray095 — Вчера, в 16:45
qq
FADE365 — Вчера, в 19:37
Шрифт хорошо подошёл
Ray095 — Вчера, в 19:37
звони
FADE365 — Вчера, в 19:37
Я щас не у компа
Ray095 — Вчера, в 19:37
оки
Ray095 — Вчера, в 21:10
Добавь pally.info
то я что то делаю верефикацию - не хочет 
я видео посмотрел о paypal - допизды
я добавил конечно все хз как но добавил,триллион видео пересмотрел о этом как добавить 
и ска -не хочет работать
stripe советуют кст, но что бы его сделать надо быть в США вообщем...
А так я даже не знаю что делать..
Ray095 — Вчера, в 21:18
Проверишь конечно оплату через paypal , я добавил - но она не работает что бы знал, потом уберу 
FADE365 — Вчера, в 21:39
Нафиг это нужно
Пэйпалыч усё решит
Там есть оплата через это все
Ray095 — Вчера, в 21:41
Не получается у меня с ним
Попробуй ты
Ray095 — Вчера, в 21:42
Я так для разнообразие попробовал
FADE365 — Вчера, в 21:42
Ок
Ray095 — Вчера, в 21:42
Отпишись потом,  я регистрацию сделал но чёт верификацию не могу пройти
FADE365 — Вчера, в 21:44
Ок
Ray095 — Вчера, в 22:31
Когда дома будешь ?
FADE365 начал звонок, который продлился 2 часа. — Вчера, в 22:38
FADE365 — Вчера, в 22:44
Изображение
Ray095 — Вчера, в 22:50
Изображение
FADE365 — Вчера, в 22:53
https://git-scm.com/downloads
FADE365 — Вчера, в 23:06
https://ray0955.github.io/9b9t.github.io/9b9t/index.html
Ray095 — Вчера, в 23:07
Изображение
FADE365 — Вчера, в 23:21
https://ray0955.github.io/9b9t.github.io/9b9t/index.html
https://ru.stackoverflow.com/questions/1552667/gsap-Не-повторяет-анимацию-при-повторном-заходе-на-страницу
Stack Overflow на русском
GSAP Не повторяет анимацию при повторном заходе на страницу
Анимация проигрывается пока я нахожусь на странице впервые, после того как я выхожу и захожу обратно - анимация перестает проигрываться
Я пробовал:

timeline ложить в useRef
gsap.context и в дальне...
Изображение
https://www.bing.com/ck/a?!&&p=ed718e62c7c72b3a5a728cac912b05a405138f523089ff306f0050ca3497b98dJmltdHM9MTc0MDE4MjQwMA&ptn=3&ver=2&hsh=4&fclid=1376086f-71fd-6247-3d86-1d25709e63be&psq=GSAP+не+подключен.+Эффекты+свечения+не+будут+работать.&u=a1aHR0cHM6Ly9xbmEuaGFici5jb20vcS8xMjE0NDky&ntb=1
Ray095 — Вчера, в 23:30
Изображение
FADE365 — Вчера, в 23:36
Изображение
Изображение
https://github.com/FADE365/DELTA-MARKET/assets/127807240/969e76a6-7139-48a8-bffe-21b526f9fb65
FADE365 — Вчера, в 23:58
@keyframes rgb-border {
    0% {
        border-color: var(--dark-color);
    }
    1% {
        border-color: var(--dark-color);
    }
    16% {
        border-color: rgba(227, 0, 255, 0.68);
    }
    32% {
        border-color: var(--dark-color);
    }
    48% {
        border-color: rgba(0, 255, 217, 0.91);
    }
    64% {
        border-color: var(--dark-color);
    }
    80% {
        border-color: rgba(227, 0, 255, 0.68);
    }
    96% {
        border-color: var(--dark-color);
    }
    100% {
        border-color: var(--dark-color);
    }
}
.background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('../images/img3.akspic.ru-zvezda-kosmicheskoe_prostranstvo-elektrik-nebo-mlechnyj_put-7836x5237.jpg') no-repeat center;
    background-size: cover;
    filter: blur(0px);
    z-index: -1;
}
Ray095 — Сегодня, в 00:07
Изображение
Изображение
FADE365 — Сегодня, в 00:09
Изображение
FADE365 — Сегодня, в 01:56
Изображение
ну вот и усё
я сделал как должно быть
Изображение
FADE365 — Сегодня, в 02:52
вот скрипт по добавлению продуктов запрошенных с бд
    // Загрузка продуктов с сервера
    fetch("https://9b9t.shop:8443/api/products")
        .then(response => response.json())
        .then(products => {
            // Очистить контейнер перед добавлением новых продуктов
            productsContainer.innerHTML = '';

            // Добавление продуктов на страницу
            products.forEach(product => {
                const productCard = `
                    <div class="product card" data-id="${product.id}" data-price="${product.price}" data-category="Расходники">
                        <img src="${product.imageUrl}" alt="${product.nameRu}" class="card__img">
                        <div class="card__data">
                            <h1 class="card__title" data-lang="ru">${product.nameRu}</h1>
                            <h1 class="card__title" data-lang="uk">${product.nameUk}</h1>
                            <h1 class="card__title" data-lang="en">${product.nameEn}</h1>
                            <span class="card__price" data-lang="ru">${product.price}$</span>
                            <span class="card__price" data-lang="uk">${product.price}$</span>
                            <span class="card__price" data-lang="en">${product.price}$</span>
                            <p class="card__description" data-lang="ru">${product.descriptionRu}</p>
                            <p class="card__description" data-lang="uk">${product.descriptionUk}</p>
                            <p class="card__description" data-lang="en">${product.descriptionEn}</p>
                            <button class="glow-button">
                                <span data-lang="ru">Добавить в корзину</span>
                                <span data-lang="uk">Додати до кошика</span>
                                <span data-lang="en">Add to Cart</span>
                            </button>
                        </div>
                    </div>
                `;
                productsContainer.innerHTML += productCard;
            });
        })
        .catch(error => console.error('Ошибка при загрузке продуктов:', error));
Свернуть
example.js
3 кб
вставь куды следует
FADE365 — Сегодня, в 02:59
https://9b9t.shop/9b9t/index.html
Есть проблема в подгрузке шрифта
Вот как выглядит нормальная центровка при большем колличестве товаров
Изображение
Изображение
FADE365 — Сегодня, в 03:06
вот что именно я хотел
Ray095 — Сегодня, в 07:45
На сайте зарегистрировался ?
На палычн
FADE365 — Сегодня, в 07:45
Забыл
Ray095 — Сегодня, в 11:35
вообщем . На сайте делать нету что без БД , жду подключение и тп. А исправлять мелкие недосчеты - невижу смысла 
FADE365 — Сегодня, в 15:34
так бд есть
просто нужен скрипт который с него запросит что либо
FADE365 — Сегодня, в 15:35
.
.
FADE365 — Сегодня, в 15:35
.
Ray095 — Сегодня, в 15:56
сервер работает?
что бы товары добавились
Ray095 — Сегодня, в 16:05
Не совсем понимаю с этой части что мне делать.. 
Ray095 — Сегодня, в 16:09
Если бы я знал бы как его сделать этот Бэккенд , я бы не просил бы тебя писать бы его) А так я хз 
FADE365 — Сегодня, в 16:41
Сервер работает
Я тебе скинул скрипт для джэса
Его вставляешь и он подгружает товары
Ray095 — Сегодня, в 16:45
А в html Что
ничего не делать?
FADE365 — Сегодня, в 16:46
Не чё
Ray095 — Сегодня, в 17:08
Ошибку дает
FADE365 — Сегодня, в 17:09
Чё пишет
Ray095 — Сегодня, в 17:09
script.js:80 Ошибка при загрузке продуктов: TypeError: Cannot set properties of null (setting 'innerHTML')
    at script.js:38:41
(anonymous) @ script.js:80Understand this errorAI
script.js:81 Uncaught (in promise) TypeError: Cannot set properties of null (setting 'innerHTML')
    at script.js:81:41
FADE365 начинает звонок. — Сегодня, в 17:11
FADE365 — Сегодня, в 17:12
document.addEventListener('DOMContentLoaded', () => {
    const languageSelect = document.getElementById('language-select');
    const addToCartButtons = document.querySelectorAll('.glow-button');
    const cartCount = document.querySelector('.cart-count');
    const searchInput = document.getElementById('search-input');
    const filterIcon = document.getElementById('filter-icon');
Раскрыть
example.js
10 кб
﻿
document.addEventListener('DOMContentLoaded', () => {
    const languageSelect = document.getElementById('language-select');
    const addToCartButtons = document.querySelectorAll('.glow-button');
    const cartCount = document.querySelector('.cart-count');
    const searchInput = document.getElementById('search-input');
    const filterIcon = document.getElementById('filter-icon');
    const filterDropdown = document.getElementById('filter-dropdown');
    const productsContainer = document.querySelector('.products');

    // Загрузка продуктов с сервера
    fetch("https://9b9t.shop:8443/api/products")
        .then(response => response.json())
        .then(products => {
            // Очистить контейнер перед добавлением новых продуктов
            productsContainer.innerHTML = '';

            // Добавление продуктов на страницу
            products.forEach(product => {
                const productCard = `
                    <div class="product card" data-id="${product.id}" data-price="${product.price}" data-category="${product.category}">
                        <img src="${product.imageUrl}" alt="${product.nameRu}" class="card__img">
                        <div class="card__data">
                            <h1 class="card__title" data-lang="ru">${product.nameRu}</h1>
                            <h1 class="card__title" data-lang="uk">${product.nameUk}</h1>
                            <h1 class="card__title" data-lang="en">${product.nameEn}</h1>
                            <span class="card__price" data-lang="ru">${product.price}$</span>
                            <span class="card__price" data-lang="uk">${product.price}$</span>
                            <span class="card__price" data-lang="en">${product.price}$</span>
                            <p class="card__description" data-lang="ru">${product.descriptionRu}</p>
                            <p class="card__description" data-lang="uk">${product.descriptionUk}</p>
                            <p class="card__description" data-lang="en">${product.descriptionEn}</p>
                            <button class="glow-button">
                                <span data-lang="ru">Добавить в корзину</span>
                                <span data-lang="uk">Додати до кошика</span>
                                <span data-lang="en">Add to Cart</span>
                            </button>
                        </div>
                    </div>
                `;
                productsContainer.innerHTML += productCard;
            });
        })
        .catch(error => console.error('Ошибка при загрузке продуктов:', error));

    // Обработчик для выбора языка
    const savedLanguage = localStorage.getItem('selectedLanguage') || 'ru';
    languageSelect.value = savedLanguage;
    changeLanguage(savedLanguage);

    languageSelect.addEventListener('change', (event) => {
        const selectedLang = event.target.value;
        localStorage.setItem('selectedLanguage', selectedLang);
        changeLanguage(selectedLang);
    });

    function changeLanguage(lang) {
        // Обновляем текст кнопки "Добавить в корзину"
        addToCartButtons.forEach(button => {
            const spans = button.querySelectorAll('span');
            spans.forEach(span => {
                if (span.getAttribute('data-lang') === lang) {
                    span.style.display = 'inline';
                } else {
                    span.style.display = 'none';
                }
            });
        });

        // Обновляем текст других элементов
        document.querySelectorAll('[data-lang]').forEach(element => {
            if (element.getAttribute('data-lang') === lang) {
                element.style.display = 'block';
            } else {
                element.style.display = 'none';
            }
        });
    }

    // Обработчик для кнопок "Добавить в корзину"
    addToCartButtons.forEach(button => {
        button.addEventListener('click', () => {
            // Добавляем класс для анимации
            button.classList.add('clicked');

            // Убираем класс после завершения анимации
            setTimeout(() => {
                button.classList.remove('clicked');
            }, 500);

            // Логика добавления товара в корзину
            const product = button.closest('.product');
            const productId = product.getAttribute('data-id');
            const productName = product.querySelector('.card__title').innerText;
            const productPrice = parseFloat(product.getAttribute('data-price'));
            const productImage = product.querySelector('.card__img').src;

            let cart = JSON.parse(localStorage.getItem('cart')) || [];

            // Проверяем, есть ли товар в корзине
            const existingItem = cart.find(item => item.id === productId);
            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({
                    id: productId,
                    name: productName,
                    price: productPrice,
                    image: productImage,
                    quantity: 1
                });
            }

            // Сохраняем корзину в localStorage
            localStorage.setItem('cart', JSON.stringify(cart));

            // Обновляем счетчик корзины
            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
            cartCount.innerText = totalItems;
        });
    });

    // Инициализация счетчика корзины
    const cart = JSON.parse(localStorage.getItem('cart')) || [];
    const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
    cartCount.innerText = totalItems;

    // Обработчик для поиска
    searchInput.addEventListener('input', () => {
        const searchTerm = searchInput.value.toLowerCase();
        const products = document.querySelectorAll('.product.card');
        products.forEach(product => {
            const title = product.querySelector('.card__title').innerText.toLowerCase();
            product.style.display = title.includes(searchTerm) ? 'block' : 'none';
        });
    });

    // Показ/скрытие выпадающего списка фильтров
    filterIcon.addEventListener('click', (e) => {
        e.stopPropagation();
        filterDropdown.classList.toggle('active');
    });

    // Закрытие выпадающего списка при клике вне его области
    document.addEventListener('click', (e) => {
        if (!filterDropdown.contains(e.target) && !filterIcon.contains(e.target)) {
            filterDropdown.classList.remove('active');
        }
    });

    // Обработчик для выбора фильтра
    filterDropdown.querySelectorAll('.filter-option').forEach(option => {
        option.addEventListener('click', () => {
            const filter = option.getAttribute('data-filter');
            filterDropdown.classList.remove('active');

            const products = document.querySelectorAll('.product.card');
            products.forEach(product => {
                const category = product.getAttribute('data-category');
                if (filter === 'all' || category === filter) {
                    product.style.display = 'block';
                } else {
                    product.style.display = 'none';
                }
            });
        });
    });

    // Генерация эффекта свечения для кнопок
    generateGlowButtons();
});

const generateGlowButtons = () => {
    document.querySelectorAll(".glow-button").forEach((button) => {
        let gradientElem = button.querySelector('.gradient');

        if (!gradientElem) {
            gradientElem = document.createElement("div");
            gradientElem.classList.add("gradient");
            button.appendChild(gradientElem);
        }

        button.addEventListener("pointermove", (e) => {
            const rect = button.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;

            gsap.to(button, {
                "--pointer-x": `${x}px`,
                "--pointer-y": `${y}px`,
                duration: 0.6,
            });

            gsap.to(button, {
                "--button-glow": chroma
                    .mix(
                        getComputedStyle(button)
                            .getPropertyValue("--button-glow-start")
                            .trim(),
                        getComputedStyle(button)
                            .getPropertyValue("--button-glow-end")
                            .trim(),
                        x / rect.width
                    )
                    .hex(),
                duration: 0.2,
            });
        });
    });
};
